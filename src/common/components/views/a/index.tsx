import React from 'react'
import type { ReactElement } from 'react'
import Greeting from '../../example/greeting'

/**
 * Display the component for the view A.
 */
export default function A (): ReactElement {
  return <Greeting />
}
