import React from 'react'
import type { ReactElement } from 'react'
import Counter from '../../example/counter'

/**
 * Display the component for the view B.
 */
export default function B (): ReactElement {
  return <Counter />
}
